import React, { Component } from 'react';
import Body from './Body';

export default class Home extends Component {
    render() {
        return (
            <div>
                <Body />
            </div>
        );
    }
}
